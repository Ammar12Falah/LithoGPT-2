# Freeze-closure rebuild environment -- NOT the ingestion environment.
# Captured 2026-07-20 (clean venv); minimal closure that regenerates corpus_manifest byte-identically.
# Ingestion-time parser versions (pyarrow/lasio/dlisio/scipy) were NOT recorded; frozen parquets are ground truth.
numpy==1.26.3
pandas==2.2.3
python-dateutil==2.9.0.post0
pytz==2026.2
six==1.17.0
tzdata==2026.3
