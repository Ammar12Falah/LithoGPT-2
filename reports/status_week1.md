# Week 1 status note

Shipped: repo scaffold + CI + tests (15 passing, ruff clean); handoff + 4 companion docs committed to docs/, mnemonic_aliases.yaml in configs/; config loader, LAS reader, harmonize.py (unit-convert before range gates, log10 resistivities, gate-to-missing, unmapped-mnemonic logging) all unit-tested; polite resumable checksummed fetcher; FORCE 2020 + NLOG ingesters (KGS skeleton for week 2); FORCE penalty_matrix.npy (12x12) + starter_notebook.ipynb pinned with sha256 in configs/force2020/pinned.json; docs/LICENSE_MATRIX.md with per-source terms citations.
Blocked: nothing blocks week-1 shipping. Live bulk ingestion cannot run in this build container (its network reaches GitHub and PyPI only, not zenodo.org / nlog.nl / kgs.ku.edu); ingesters are written to run in Ammar's environment.
Next (week 2, toward G1): run FORCE ingest against Zenodo and verify the 98 train count; obtain a confirmed NLOG Datacenter index and start the resumable bulk NLOG job (expected wall-clock: hours); build KGS ingester; QC suite v1 + dashboards; dedup v1.
Spend cumulative: 0 A40-hours, 0 USD. RunPod credit confirmed at 400 USD, well above the 66 USD planning cap (150 A40-hours).
Unmapped mnemonics: 0 (the log populates once ingesters run against real LAS).
UNVERIFIED / ASSUMPTION introduced this week: FORCE 98-well train count is UNVERIFIED here (Zenodo unreachable from this container; the GitHub copy is a 28-well team subset, not the official set); NLOG raw-redistribution UNCLEAR; KGS raw-redistribution UNCLEAR/likely restricted (some KGS-hosted data is IHS-licensed); NLOG bulk-index endpoint PENDING (not guessed); the HF-hub open-weights check was attempted via the HF hub tool but needs interactive approval, so the "no open-weights equivalent" hedge stays open; resample policy is nearest-sample-within-half-a-grid-step (ASSUMPTION, v1, flagged for G1 review).
